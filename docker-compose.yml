# THIS DOCKERFILE IS ONLY INTENDED FOR TESTING
version: '3.7'
services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        PORT: 5000
    ports:
      - '5000:5000'
